# atv-apiRest
sqlite/node/express

http para testes de get:

busca por id -> http://localhost:3000/produtos/(id)
busca por nome do produto -> http://localhost:3000/produtos/busca/nome?q=(nome)
busca por faixa maxima de preco -> http://localhost:3000/produtos/filtro/preco?max=(valor)
